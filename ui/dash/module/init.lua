return {
  stat = require(... .. '.stat'),
  song = require(... .. '.song'),
  weather = require(... .. '.weather'),
  todo = require(... .. '.todos'),
  quote = require(... .. '.quote'),
  nf = require(... .. '.nf'),
  profile = require(... .. '.profile'),
}
