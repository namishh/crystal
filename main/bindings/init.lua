return {
  client = {
    key = require 'main.bindings.client.key',
    mouse = require 'main.bindings.client.mouse',
  },
  global = {
    key = require 'main.bindings.global.key',
    mouse = require 'main.bindings.global.mouse',
  },
  mod    = require 'main.bindings.mod'
}
